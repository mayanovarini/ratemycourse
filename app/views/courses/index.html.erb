<div class="container">
  <div class ="jumbotron">
    <div class="row">
      <div class="col-xs-6">
        <h1 style="font-size: 35px">I want to rate my course for</h1>
      </div>
      <div class="col-xs-3">
        <input type="text" class="form-control" placeholder="Department Name">
      </div>
      <div class="col-xs-2">
        <input type="text" class="form-control" placeholder="Course Code">
      </div>
    </div>
    <button type="submit" class="btn btn-default">Search</button>
  </div>

<table>
  <% @courses.each do |course| %>
    <tr>
      <td><%= link_to course.code, course %></td>
    </tr>
    <tr>
      <td><%= link_to course.name, course %></td>
    </tr>
    <tr>
      <td><%= course.department %> Department</td>
    </tr>
        <tr><%= link_to 'Edit', edit_course_path(course) %></tr>
        <tr><%= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' } %></tr>
        <tr>City College of San Francisco</tr>
        #put rating here

      </td>

      <td>
        #review here
      </td>
    </tr>
  <% end %>
</table>

<%= link_to 'New Course', new_course_path %>
</div>
